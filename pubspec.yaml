name: access_gate
description: "Hide Flutter widgets behind feature flags, roles, permissions, and attribute-based access policies."
version: 0.0.5
repository: https://github.com/david-k-williams/access_gate
issue_tracker: https://github.com/david-k-williams/access_gate/issues
topics:
  - authorization
  - feature-flags
  - rbac
  - abac

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ">=2.0.3 <7.0.0"

flutter:
